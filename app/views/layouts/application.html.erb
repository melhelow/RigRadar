<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Rails 8 Template" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" defer></script>

  </head>

  <body>
  <p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<nav class="navbar navbar-expand-md navbar-light bg-light mb-3 border-bottom">
  <div class="container">
    <%= link_to "RigRadar", root_path, class: "navbar-brand fw-semibold" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav"
      aria-controls="mainNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="mainNav">
      <ul class="navbar-nav me-auto mb-2 mb-md-0">
        <% if driver_signed_in? %>
          <li class="nav-item"><%= link_to "Loads", loads_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "Truck Stops", truck_stops_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "Rest Areas", rest_areas_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "Weigh Stations", weigh_stations_path, class: "nav-link" %></li>
        <% end %>
      </ul>

      <div class="d-flex gap-2">
        <% if driver_signed_in? %>
          <%= link_to "Account", edit_driver_registration_path, class: "btn btn-outline-secondary btn-sm" %>
          <%= button_to "Sign out", destroy_driver_session_path, method: :delete,
                class: "btn btn-danger btn-sm" %>
        <% else %>
          <%= link_to "Sign in", new_driver_session_path, class: "btn btn-outline-primary btn-sm" %>
          <%= link_to "Get started", new_driver_registration_path, class: "btn btn-primary btn-sm" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>

<div class="container">
  <% if notice.present? %>
    <div class="alert alert-success"><%= notice %></div>
  <% end %>
  <% if alert.present? %>
    <div class="alert alert-danger"><%= alert %></div>
  <% end %>
  <%= yield %>
</div>


  </body>
</html>
