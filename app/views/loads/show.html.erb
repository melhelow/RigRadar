<h1>Load <%= @load.id %></h1>
<p><strong>Commodity:</strong> <%= @load.commodity %> — <%= @load.weight_lbs %> lbs</p>
<p><strong>Pickup:</strong> <%= @load.pickup_location %></p>
<p><strong>Drop-off:</strong> <%= @load.dropoff_location %></p>
<p><strong>Status:</strong> <%= @load.status %></p>

<p>
  <%= link_to "Plan Route", plan_load_path(@load), class: "btn btn-secondary" %>
  <%= button_to "Start",    start_load_path(@load),    method: :patch, class: "btn btn-primary" %>
  <%= button_to "Deliver",  deliver_load_path(@load),  method: :patch, class: "btn" %>
  <%= button_to "Drop",     drop_load_path(@load),     method: :patch, class: "btn" %>
  <%= button_to "Refresh Coordinates", regeocode_load_path(@load), method: :patch %>

</p>
<p>
  <%= link_to "Edit", edit_load_path(@load) %> |
  <%= button_to "Delete", load_path(@load), method: :delete,
        data: { turbo_confirm: "Delete this load?" } %> |
  <%= link_to "Back to Loads", loads_path %>
</p>
<p><%= link_to "Back to Loads", loads_path %></p>
